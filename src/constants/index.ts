// Navigation delays
export const NAVIGATION_DELAY_MS = 100;

// API timeouts
export const API_TIMEOUT_MS = 1000000;

// Cache settings
export const QUERY_STALE_TIME_MS = 5 * 60 * 1000; // 5분
export const DEFAULT_RETRY_COUNT = 1;

// Auth settings
export const AUTH_TOKEN_EXPIRES_DAYS = 7;

// Team settings
export const MAX_TEAM_SCORE = 50;

// UI pagination
export const MAX_DISPLAYED_USERS = 10;
export const MAX_PLAYERS_HEIGHT_CLASS = 'max-h-96';

// Loading states
export const MINIMUM_LOADING_DELAY_MS = 500; 